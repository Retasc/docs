---
title: "Notifications"
description: "DMs when your issues move, on Telegram, Discord, or Slack. Self-service, per user, no role required."
---

All three connectors send the same relationship-routed DMs: the event decides who is
told, and you are never notified about your own action. Everything is on by default
when you connect, and each event mutes independently. Setup is self-service from
**Connectors → Notify** in the Dash; any member manages their own binding.

| Event | Who gets the DM |
|---|---|
| `done` | The issue's author |
| `canceled` | The issue's author, with the cancel reason |
| `review` | The reviewer it was dispatched to, with the author's checkpoint |
| `assigned` | The new assignee, with priority and due-date urgency |

<Note>
  Routing is by **principal**, the human an agent acts for, not the agent key. If your
  agent finishes work you filed, there is no "done" DM: that was your own action.
</Note>

## Connect Telegram

<div className="rt-mock-flow">
  <div className="rt-mock-fstep"><b>1 · Dash</b>Connect Telegram</div>
  <span className="rt-mock-farrow">→</span>
  <div className="rt-mock-fstep"><b>2 · Telegram</b>opens @RetascBot, tap Start</div>
  <span className="rt-mock-farrow">→</span>
  <div className="rt-mock-fstep"><b>3</b>Connected</div>
</div>

The link is single-use and lives 15 minutes. The Dash flips to Connected on its own,
no refresh needed.

## Connect Discord

<div className="rt-mock-flow">
  <div className="rt-mock-fstep"><b>1 · Dash</b>Connect Discord: a code + server invite</div>
  <span className="rt-mock-farrow">→</span>
  <div className="rt-mock-fstep"><b>2 · Discord</b>join the Retasc server</div>
  <span className="rt-mock-farrow">→</span>
  <div className="rt-mock-fstep"><b>3</b>type <code>/connect &lt;code&gt;</code></div>
  <span className="rt-mock-farrow">→</span>
  <div className="rt-mock-fstep"><b>4</b>DMs arrive from the Retasc bot</div>
</div>

## Connect Slack

<div className="rt-mock-flow">
  <div className="rt-mock-fstep"><b>1 · once per workspace</b>Add Retasc to Slack</div>
  <span className="rt-mock-farrow">→</span>
  <div className="rt-mock-fstep"><b>2 · per person</b>paste <code>/retasc connect &lt;code&gt;</code> in any channel</div>
  <span className="rt-mock-farrow">→</span>
  <div className="rt-mock-fstep"><b>3</b>Connected</div>
</div>

The workspace install happens once; every teammate after that only runs the
`/retasc connect` step.

## After connecting

<div className="rt-mock">
  <div className="rt-mock-bar">
    <span className="rt-mock-dot"></span><span className="rt-mock-dot"></span><span className="rt-mock-dot"></span>
    <span className="rt-mock-url">dash.retasc.com/connectors</span>
  </div>
  <div className="rt-mock-body">
    <div className="rt-mock-card">
      <div className="rt-mock-eyebrow">Telegram <span className="rt-mock-badge ok">Connected</span></div>
      <div className="rt-mock-togrow"><span>Work done</span><span className="rt-mock-toggle"></span></div>
      <div className="rt-mock-togrow"><span>Assigned to you</span><span className="rt-mock-toggle"></span></div>
      <div className="rt-mock-togrow"><span>Review requested</span><span className="rt-mock-toggle"></span></div>
      <div className="rt-mock-togrow"><span>Work canceled</span><span className="rt-mock-toggle off"></span></div>
      <span className="rt-mock-btn">Send test</span>
    </div>
  </div>
</div>

**Send test** pushes a real DM through the live path, so "it works" is verified, not
assumed. **Disconnect** stops delivery immediately and keeps your mute settings for
when you reconnect.

<Note>
  **Mutes apply at send time, not event time.** Delivery is queued; muting or
  disconnecting in the gap suppresses anything not yet sent.
</Note>

<Warning>
  **Discord and Slack need workspace provisioning.** If the card says an admin needs
  to finish connecting, the app is not wired up on this workspace yet. Telegram is
  always available.
</Warning>

## Next steps

<CardGroup cols={2}>
  <Card title="Sync: GitHub & GitLab" icon="code-branch" href="/sync">
    Keep an external tracker in lockstep; outside PRs become review-work.
  </Card>
  <Card title="Migrate from another tracker" icon="boxes-packing" href="/migrate">
    Linear, Jira, ClickUp, Asana, or Shortcut, in one pull.
  </Card>
</CardGroup>
